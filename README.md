# multi-document-pipeline
Multi‑PDF audit pipeline: extract claims via LLM, cluster semantically, detect supports/contradictions with KNN + negation pre‑screen + LLM mid‑band + transitive inference, then score via PageRank, source authority, bursts &amp; lifecycle.
